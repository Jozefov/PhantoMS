name: phantoms_env
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.11
  - pip
  - pip:
    - torch
    - pytorch-lightning
    - torch-geometric
    - wandb
    - tensorboard
    - 'massspecgym[notebooks] @ git+ssh://git@github.com/Jozefov/MassSpecGymMSn.git@main'
    - -e .